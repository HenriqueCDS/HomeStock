# Database
DB_NAME=stockflow
DB_URL=jdbc:postgresql://localhost:5432/stockflow
DB_USERNAME=stockflow
DB_PASSWORD=change_me_in_production

# JWT
JWT_SECRET=generate-a-random-256-bit-secret-here-use-openssl-rand-base64-64
JWT_ACCESS_EXP_MS=900000
JWT_REFRESH_EXP_MS=604800000

# Spring
SPRING_PROFILES_ACTIVE=prod

# App version (for prod docker-compose)
APP_VERSION=1.0.0
